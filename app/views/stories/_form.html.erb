<%= form_for(@story) do |f| %>
  <% if @story.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@story.errors.count, "error") %> prohibited this story from being saved:</h2>

      <ul>
      <% @story.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :point %><br>
    <%= f.number_field :point %>
  </div>
  <div class="field">
    <%= f.label :stages %><br>
    <% option = ['Analysis','Ready For Dev','In Dev',
                 'Dev Complete','In Test','Complete'] %>
    <%= select("story", "stages", option.collect{ |c| [c, c] }) %>
  </div>
    <div class="field">
      <%= f.label :project %><br />
      <%= select("story", "project_id", Project.all.collect{ |c| [ c.title, c.id] }) %>
    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
